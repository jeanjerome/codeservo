# What measures a maintainer's commit.
#
# The control loop measures a candidate; nothing measured this repository's own
# commits. A documentation change, a generation checkpoint or a constitution
# change was verified only at the baseline of the next run, so detection was
# deferred and it was the run that failed rather than the commit.
name: checks

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # The audit reads a database that moves without this repository moving.
    - cron: "17 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

env:
  # Pinned rather than resolved, like every tool a measurement reaches for.
  # The manifest requires >=0.77,<0.78.
  PIXI_VERSION: 0.77.1

jobs:
  gates:
    # The same gates under both mechanisms. A profile says what a process may
    # not reach; which mechanism enforces it belongs to the host, so measuring
    # on one operating system would leave the other one claimed rather than
    # measured. The workspace also declares osx-64, which the lockfile check
    # resolves and nothing runs.
    strategy:
      # One host failing is a result about that host, and hiding the other's
      # would turn two measurements into one.
      fail-fast: false
      matrix:
        os: [macos-15, ubuntu-24.04]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - name: The mechanism this host confines with
        if: runner.os == 'Linux'
        # Ubuntu 24.04 restricts unprivileged user namespaces through AppArmor,
        # and the bubblewrap package installs no profile of its own, so bwrap
        # fails setting up its uid map and does not start at all. Lifting the
        # restriction is what a Linux host running the controller has to do,
        # and it is a property of the host rather than of this repository. The
        # controller establishes its mechanism by applying a profile, so a host
        # where this step were skipped refuses the run rather than executing it
        # unconfined.
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq bubblewrap
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
          bwrap --dev-bind / / -- /bin/sh -c 'echo a profile applies here'

      - name: Install Pixi
        run: |
          curl -fsSL https://pixi.sh/install.sh | bash
          echo "$HOME/.pixi/bin" >> "$GITHUB_PATH"

      - name: The lockfile agrees with the manifest
        # A maintainer command, and this is a maintainer's checkout: where the
        # two disagree it exits 1 and rewrites pixi.lock. The exit code is the
        # verdict and the rewrite dies with the runner.
        run: pixi lock --check --no-config

      - name: Install the locked environment
        run: pixi install --locked --no-config

      - name: This workflow runs every gate the constitution declares
        # The gates below are named twice, in the constitution and here. A gate
        # added to one and not the other would leave a green workflow that
        # measures less than the loop does.
        run: |
          pixi run --locked --no-config python -c '
          import pathlib, sys, tomllib
          text = pathlib.Path(".codeservo/constitution.toml").read_text()
          declared = {g["task"] for g in tomllib.loads(text)["gate"] if "task" in g}
          here = set(sys.argv[1:])
          if declared != here:
              sys.exit(f"the constitution declares {sorted(declared)}, this workflow runs {sorted(here)}")
          print(f"both name {sorted(declared)}")
          ' lint types test compile architecture coverage fuzz duration

      - run: pixi run --locked --no-config lint
      - run: pixi run --locked --no-config types
      - run: pixi run --locked --no-config test
      - run: pixi run --locked --no-config compile
      - run: pixi run --locked --no-config architecture
      - run: pixi run --locked --no-config coverage
      - run: pixi run --locked --no-config fuzz
      - run: pixi run --locked --no-config duration

      - name: The record is what the committed reference says it is
        # Record parity was a manual discipline, so a forgettable one. The
        # reference is a control input: a change that moves the record moves it
        # too, in the same commit, where the diff says what moved.
        run: |
          pixi run --locked --no-config python tools/record_parity.py capture "$RUNNER_TEMP/head.json"
          pixi run --locked --no-config python tools/record_parity.py compare tools/record_parity.reference.json "$RUNNER_TEMP/head.json"

  mutation:
    # Only on the schedule and on demand. It takes about twelve minutes, and it
    # can never be a gate anyway: it edits the source to make each mutant, so
    # it modifies the tree it measures. What it holds is a ratchet — the tests
    # covering the decision core may not get quieter than they are.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7

      - name: Install Pixi
        run: |
          curl -fsSL https://pixi.sh/install.sh | bash
          echo "$HOME/.pixi/bin" >> "$GITHUB_PATH"

      - name: Install both environments
        run: pixi install --all --locked --no-config

      - name: The decision core is held by the tests that cover it
        run: pixi run -e mutation --locked --no-config mutation

  fuzz-search:
    # The gate searches each boundary on a fixed budget with a fixed seed, so
    # it answers the same way twice and holds the crashes already found. This
    # is the other mode: a wall clock instead of a run count, and a seed the
    # fuzzer draws itself, which is where a new input is found. Like the audit
    # it can go red without this repository moving, and what it finds becomes
    # a named case in the suite rather than a longer budget here.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7

      - name: Install Pixi
        run: |
          curl -fsSL https://pixi.sh/install.sh | bash
          echo "$HOME/.pixi/bin" >> "$GITHUB_PATH"

      - name: Install the locked environment
        run: pixi install --locked --no-config

      - name: The boundaries answer for longer than the gate searches them
        run: pixi run --locked --no-config python tools/fuzz.py --search 600

  audit:
    # Its own job because it is the one check that can go red without this
    # repository moving: an advisory published overnight is a real signal and
    # not a defect of the commit under test.
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7

      - name: Install Pixi
        run: |
          curl -fsSL https://pixi.sh/install.sh | bash
          echo "$HOME/.pixi/bin" >> "$GITHUB_PATH"

      - name: Install both environments
        # The audited environment is the one the gates run through, so this job
        # installs it as well as the one that audits it. Installing only the
        # auditor would leave nothing to audit, and the task's path check is
        # what turns that into a failure instead of a green step measuring an
        # empty directory.
        run: pixi install --all --locked --no-config

      - name: The environment's Python distributions carry no known advisory
        run: pixi run -e audit --locked --no-config audit
